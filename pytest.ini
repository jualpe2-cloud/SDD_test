[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
pythonpath = .

markers =
    auth: authentication tests
    routes: route endpoint tests
    gpx: GPX export tests
    integration: integration tests
    unit: unit tests
